# Variables de Entorno para Producción
# Copia este archivo y renómbralo a .env en tu servicio de hosting (Render, Railway, etc.)

# Puerto (Render usa 10000 por defecto, otros servicios pueden variar)
PORT=10000

# Base de Datos MySQL (PlanetScale, Railway, o el servicio que uses)
DB_HOST=tu-host-de-mysql.planetscale.com
DB_PORT=3306
DB_NAME=personajes
DB_USER=tu-usuario
DB_PASSWORD=tu-contraseña

# JWT Secret (genera uno seguro: openssl rand -base64 32)
JWT_SECRET=tu-secreto-super-seguro-aqui-cambiar-en-produccion

# Entorno
NODE_ENV=production

# URL del Frontend (Vercel, Netlify, etc.)
FRONTEND_URL=https://tu-app.vercel.app
