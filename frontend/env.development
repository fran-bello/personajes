# Variables de Entorno para Frontend en Desarrollo
# Estas variables se usan cuando ejecutas `npm run dev`

# URL del Backend API (desarrollo local)
VITE_API_URL=http://localhost:3001/api

# URL del Socket.io (mismo que el backend pero sin /api)
VITE_SOCKET_URL=http://localhost:3001
